This model, after training, is able to classify the genre of a piece of music.

It works by converting the audio file (.wav/.mp3) into a mel-spectrogram image, which is then fed into a convolutional neural network.
